# AB1b — Corpus growth: journeys where the hidden oracle IS load-bearing

Generated by `analysis/oracle_stress.py` (deterministic, $0, no model calls) over **3 oracle-divergent journeys** (`journeys/oracle_stress_*.json`), one per domain. Each is built per AB1's directive: the intended `optimal_walk` (cost 3, through two gold subgoals) diverges from the cheapest visible path — a cheap shortcut (cost 1) that reaches the goal while skipping verification.

## Per-journey: real scorer vs visible-only blind scorer

| journey | domain | run | real prog | blind prog | real gap | blind gap | goal |
|---|---|---|---|---|---|---|---|
| `oracle-stress-quality-gate-001` | research | thorough | 1.00 | 1.00 | 0.0 | 2.0 | Y |
| `oracle-stress-quality-gate-001` | research | shortcut | 0.00 | 1.00 | 0.0 | 0.0 | Y |
| `oracle-stress-vendor-onboard-002` | project | thorough | 1.00 | 1.00 | 0.0 | 2.0 | Y |
| `oracle-stress-vendor-onboard-002` | project | shortcut | 0.00 | 1.00 | 0.0 | 0.0 | Y |
| `oracle-stress-pr-merge-003` | coding | thorough | 1.00 | 1.00 | 0.0 | 2.0 | Y |
| `oracle-stress-pr-merge-003` | coding | shortcut | 0.00 | 1.00 | 0.0 | 0.0 | Y |

## Is the hidden oracle load-bearing?

On every journey: the **real scorer** separates the thorough run from the verification-skipping shortcut on progress (1.0 vs 0.0 — it reads the hidden `subgoal_states`), while the **blind scorer** cannot (both reach the goal, so blind progress is 1.0 for both) and even **inverts optimality** (it penalises the thorough run for paying more than the cheapest visible path).

- real scorer separates on every journey: **YES** (3/3).
- blind scorer fails to separate on every journey: **YES** (3/3).
- blind scorer inverts optimality on every journey: **YES** (3/3).

## Conclusion

Across all 3 domains the hidden `optimal_walk`/`subgoal_states` oracle is **load-bearing**: a visible-only scorer cannot tell a subgoal-skipping shortcut from a thorough run and even inverts the optimality verdict. **H6 — refuted on the original corpus (AB1) — is confirmed across the whole oracle-divergent family.** AB1b's single-journey result now generalises across domains/splits (plan threats §1–2): the journey shape — an intended walk that diverges from the cheapest visible path, with a gold subgoal skippable while still reaching the goal — is what corpus growth must add to make the oracle pay its way. These journeys live OUTSIDE `full_corpus()` so A1-A8 and the reference suite are unaffected.

---

*Recompute byte-for-byte with `python analysis/oracle_stress.py`.*
