# AB1b — Corpus growth: a journey where the hidden oracle IS load-bearing

Generated by `analysis/oracle_stress.py` (deterministic, $0, no model calls) on `journeys/oracle_stress_001.json` — a quality-gate journey built per AB1's directive: `optimal_walk` (start->gather->verify->published, cost 3) diverges from the cheapest visible path (start->published, cost 1), and the cheap shortcut reaches the goal while skipping the `claims_verified` gold subgoal.

| run | real progress | blind progress | real gap | blind gap | goal reached |
|---|---|---|---|---|---|
| thorough | 1.00 | 1.00 | 0.0 | 2.0 | yes |
| shortcut | 0.00 | 1.00 | 0.0 | 0.0 | yes |

## Is the hidden oracle load-bearing here?

- **real scorer** separates the thorough run from the verification-skipping shortcut on progress: **YES** (progress 1.0 vs 0.0 — it reads the hidden `subgoal_states`).
- **blind scorer** separates them: **NO** — without `subgoal_states` the only visible signal is 'reached the goal', and both runs do, so the verification-skipping shortcut is indistinguishable from the thorough run.
- **blind scorer inverts optimality**: **YES** — using the cheapest visible path as the baseline it rates the thorough run as suboptimal (gap 2) and the shortcut as optimal (gap 0), while the real scorer (true `optimal_walk` baseline) rates both at gap 0.

## Conclusion

On this journey the hidden `optimal_walk`/`subgoal_states` oracle is **load-bearing**: a visible-only scorer cannot tell a verification-skipping shortcut from a thorough run (both reach the goal) and even inverts the optimality verdict. **H6 — refuted on the original corpus (AB1) — is confirmed here.** This is the journey shape corpus growth must add: an intended walk that diverges from the cheapest visible path, with a gold subgoal that is skippable while still reaching the goal. Generalising this pattern across domains/splits is the concrete next step for the corpus (plan threats §1–2).

---

*Recompute byte-for-byte with `python analysis/oracle_stress.py`.*
